<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F6F6F8">

    <!-- Top Section -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="220dp"
        android:background="#4EC3E0">

        <!-- Decorative Circles (optional, for style) -->
        <View
            android:layout_width="180dp"
            android:layout_height="180dp"
            android:layout_alignParentStart="true"
            android:layout_marginTop="-40dp"
            android:layout_marginStart="-40dp"
            android:background="@drawable/circle_bg" />

        <ImageView
            android:id="@+id/avatarImage"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:src="@drawable/avatar_placeholder"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="60dp"
            android:background="@drawable/rounded_button"
            android:scaleType="centerCrop" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Welcome N.H.M.Dilhara"
            android:textColor="#fff"
            android:textStyle="bold"
            android:textSize="22sp"
            android:fontFamily="sans-serif-medium"
            android:layout_below="@id/avatarImage"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="16dp" />
    </RelativeLayout>

    <!-- User Info Card -->
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="24dp"
        app:cardCornerRadius="18dp"
        app:cardElevation="6dp">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="20dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="User Information"
                android:textStyle="bold"
                android:textSize="17sp"
                android:textColor="#222"
                android:layout_alignParentStart="true"
                android:layout_alignParentTop="true" />

            <ImageView
                android:id="@+id/editButton"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@android:drawable/ic_input_add"
                android:tint="#4EC3E0"
                android:layout_alignParentEnd="true"
                android:layout_alignParentTop="true" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="\n    Username : N.H.M.Dilhara\n\n    E-mail :\n    2022t01525@stu.cmb.ac.lk"
                android:textSize="15sp"
                android:textColor="#444"
                android:layout_below="@id/editButton"
                android:layout_alignParentStart="true"
                android:layout_marginTop="16dp" />
        </RelativeLayout>
    </androidx.cardview.widget.CardView>

    <!-- Edit Info Button -->
    <Button
        android:id="@+id/editInfoButton"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginLeft="48dp"
        android:layout_marginRight="48dp"
        android:layout_marginTop="8dp"
        android:backgroundTint="#E6E0F8"
        android:text="Edit info."
        android:textColor="#6C5BA7"
        android:textStyle="bold"
        android:fontFamily="sans-serif-medium"
        android:drawableLeft="@android:drawable/ic_menu_edit"
        android:drawablePadding="8dp" />

    <!-- Sign-Out Button -->
    <Button
        android:id="@+id/signOutButton"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginLeft="48dp"
        android:layout_marginRight="48dp"
        android:layout_marginTop="16dp"
        android:backgroundTint="#4EC3E0"
        android:text="Sign-Out"
        android:textColor="#fff"
        android:textStyle="bold"
        android:fontFamily="sans-serif-medium" />

</LinearLayout> 